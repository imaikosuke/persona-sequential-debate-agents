---
alwaysApply: true
---

# Makefileコマンド一覧

プロジェクトのルートディレクトリにある `Makefile` に定義されているコマンドです。

## 📋 ヘルプ

```bash
make help    # 利用可能なコマンド一覧を表示
```

## 🚀 開発・実行コマンド

```bash
make dev-1   # Implementation 1を開発モードで実行
make dev-2   # Implementation 2を開発モードで実行
make dev-3   # Implementation 3を開発モードで実行（主要実装）
make dev-4   # Implementation 4を開発モードで実行
```

## 🔨 ビルドコマンド

```bash
make build-1   # Implementation 1をビルド
make build-2   # Implementation 2をビルド
make build-3   # Implementation 3をビルド
make build-4   # Implementation 4をビルド
```

## ▶️ 本番実行コマンド

```bash
make start-1   # Implementation 1を本番モードで実行
make start-2   # Implementation 2を本番モードで実行
make start-3   # Implementation 3を本番モードで実行
make start-4   # Implementation 4を本番モードで実行
```

## 🛠️ ユーティリティコマンド

### 依存関係のインストール

```bash
make install   # pnpm installを実行
```

### クリーンアップ

```bash
make clean     # ビルド成果物をクリーンアップ
```

以下のディレクトリを削除：

- `node_modules`（全てのディレクトリ）
- `dist`（全てのディレクトリ）
- `.mastra`（全てのディレクトリ）

### リンティング

```bash
make lint       # ESLintでコードをチェック（pnpm lint）
make lint-fix   # ESLintで自動修正（pnpm lint:fix）
```

### フォーマット

```bash
make format      # Prettierでコードフォーマットをチェック（pnpm format）
make format-fix  # Prettierでコードフォーマットを自動修正（pnpm format:fix）
```

### 型チェック

```bash
make typecheck   # TypeScriptの型チェック（pnpm typecheck）
```

### 総合チェック

```bash
make check       # 全チェック実行（lint + format + typecheck）
```

## 📝 よく使うコマンドシーケンス

### 開発開始時

```bash
make install     # 依存関係のインストール
make dev-3       # 開発モードで実行
```

### コード変更後

```bash
make check        # 全チェック実行
make lint-fix     # リント自動修正
make format-fix   # フォーマット自動修正
```

### クリーンビルド

```bash
make clean       # クリーンアップ
make install     # 再インストール
make build-3     # ビルド
make start-3     # 実行
```

### コミット前

```bash
make lint-fix     # リント自動修正
make format-fix   # フォーマット自動修正
make typecheck    # 型チェック
```
# Makefileコマンド一覧

プロジェクトのルートディレクトリにある `Makefile` に定義されているコマンドです。

## 📋 ヘルプ

```bash
make help    # 利用可能なコマンド一覧を表示
```

## 🚀 開発・実行コマンド

```bash
make dev-1   # Implementation 1を開発モードで実行
make dev-2   # Implementation 2を開発モードで実行
make dev-3   # Implementation 3を開発モードで実行（主要実装）
make dev-4   # Implementation 4を開発モードで実行
```

## 🔨 ビルドコマンド

```bash
make build-1   # Implementation 1をビルド
make build-2   # Implementation 2をビルド
make build-3   # Implementation 3をビルド
make build-4   # Implementation 4をビルド
```

## ▶️ 本番実行コマンド

```bash
make start-1   # Implementation 1を本番モードで実行
make start-2   # Implementation 2を本番モードで実行
make start-3   # Implementation 3を本番モードで実行
make start-4   # Implementation 4を本番モードで実行
```

## 🛠️ ユーティリティコマンド

### 依存関係のインストール

```bash
make install   # pnpm installを実行
```

### クリーンアップ

```bash
make clean     # ビルド成果物をクリーンアップ
```

以下のディレクトリを削除：

- `node_modules`（全てのディレクトリ）
- `dist`（全てのディレクトリ）
- `.mastra`（全てのディレクトリ）

### リンティング

```bash
make lint       # ESLintでコードをチェック（pnpm lint）
make lint-fix   # ESLintで自動修正（pnpm lint:fix）
```

### フォーマット

```bash
make format      # Prettierでコードフォーマットをチェック（pnpm format）
make format-fix  # Prettierでコードフォーマットを自動修正（pnpm format:fix）
```

### 型チェック

```bash
make typecheck   # TypeScriptの型チェック（pnpm typecheck）
```

### 総合チェック

```bash
make check       # 全チェック実行（lint + format + typecheck）
```

## 📝 よく使うコマンドシーケンス

### 開発開始時

```bash
make install     # 依存関係のインストール
make dev-3       # 開発モードで実行
```

### コード変更後

```bash
make check        # 全チェック実行
make lint-fix     # リント自動修正
make format-fix   # フォーマット自動修正
```

### クリーンビルド

```bash
make clean       # クリーンアップ
make install     # 再インストール
make build-3     # ビルド
make start-3     # 実行
```

### コミット前

```bash
make lint-fix     # リント自動修正
make format-fix   # フォーマット自動修正
make typecheck    # 型チェック
```
